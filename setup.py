from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Repository to mine and curate dataset and build a model to predict mlb games against the betting spread',
    author='peter',
    license='MIT',
)
